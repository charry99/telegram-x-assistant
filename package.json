{
  "name": "telegram-x-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/bot\" \"npm run dev -w apps/miniapp\"",
    "build": "npm run build -ws",
    "start": "npm start -w apps/api & npm start -w apps/bot",
    "db:push": "npm run generate -w apps/api && npx prisma db push",
    "db:migrate": "npm run migrate -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
